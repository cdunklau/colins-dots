# Use a different file for custom bits so we can easily
# get the distribution's latest from /etc/skel/.bashrc.
#
# Source this file in .bashrc proper to enable.

# Prompt hack (see make_prompt.py in the colins-dots repo)
_git_info_line() {
    BRANCH_NAME=$(
        git branch --list 2> /dev/null |
            sed -e '/^[^*]/d' -e 's/* \(.*\)/\1/'
    )
    if [ -n "$BRANCH_NAME" ]; then
        echo -e "\\ngit: branch=\033[38;5;1m$BRANCH_NAME$(tput sgr0)"
    fi
};

export PS1="\[\033[38;5;0m\][\[\033[38;5;6m\]\u\[\033[39m\]@\[\033[38;5;12m\]\h\[\033[38;5;0m\]] \t \[\033[38;5;2m\]\w\[\033[39m\]\[\033[49m\]\$(_git_info_line)\n--> \\$ \[$(tput sgr0)\]"

# Editor
export EDITOR=vim


# pyenv setup
PYENV_ROOT="$HOME/.pyenv"
if [ -d "$PYENV_ROOT" ]; then
    export PYENV_ROOT
    export PATH="$PYENV_ROOT/bin:$PATH"
    eval "$(pyenv init -)"
fi

# Haskell stuff
HASKELL_INIT="$HOME/.ghcup/env"
if [ -f "$HASKELL_INIT" ]; then
    source ${HASKELL_INIT}
fi

# Rust stuff
RUST_INIT="$HOME/.cargo/env"
if [ -f "$RUST_INIT" ]; then
    source ${RUST_INIT}
fi
